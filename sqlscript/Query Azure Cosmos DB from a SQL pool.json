{
	"name": "Query Azure Cosmos DB from a SQL pool",
	"properties": {
		"content": {
			"query": "-- IF (NOT EXISTS(SELECT * FROM sys.credentials WHERE name = 'romandp203'))\n--    CREATE CREDENTIAL [romandp203]\n--    WITH IDENTITY = 'SHARED ACCESS SIGNATURE', SECRET = ''\n-- GO\n\nSELECT *\nFROM OPENROWSET(​PROVIDER = 'CosmosDB',\n                CONNECTION = 'Account=romandp203;Database=AdventureWorks',\n                OBJECT = 'Sales',\n                SERVER_CREDENTIAL = 'romandp203'\n) WITH (\n    OrderID VARCHAR(10) '$.id',\n    OrderDate VARCHAR(10) '$.orderdate',\n    CustomerID INTEGER '$.customerid',\n    CustomerName VARCHAR(40) '$.customerdetails.customername',\n    CustomerEmail VARCHAR(30) '$.customerdetails.customeremail',\n    Product VARCHAR(30) '$.product',\n    Quantity INTEGER '$.quantity',\n    Price FLOAT '$.price'\n)\nAS Sales\nORDER BY OrderID;\n",
			"metadata": {
				"language": "sql"
			},
			"currentConnection": {
				"databaseName": "master",
				"poolName": "Built-in"
			},
			"resultLimit": -1
		},
		"type": "SqlQuery"
	}
}